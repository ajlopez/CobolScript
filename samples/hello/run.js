var cobs = require('../..'),    fs = require('fs');    var runtime = {    display: function(msg) {        console.log(msg);    }}function runFile(filename) {    var text = fs.readFileSync(filename).toString();    cobs.compileProgram(text).run(runtime);};process.argv.forEach(function(val) {    if (val.slice(-4) == ".cob")        runFile(val);});