var cobs = require('../..'),    http = require('http'),    fs = require('fs');var program = cobs.compileProgramFile('./factorial.cob');http.createServer(function(req, res) {    var runtime = {        display: function() {            if (arguments && arguments.length)                for (var k = 0; k < arguments.length; k++)                    if (arguments[k])                        res.write(arguments[k].toString());                                    res.write('\r\n');        }    }    program.run(runtime);        res.end();}).listen(8000);console.log('Server started, listening at port 8000');